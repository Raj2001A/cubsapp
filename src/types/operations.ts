// This file previously defined a conflicting OperationType type alias.
// OperationType should be imported from '../types/ui' as an enum for consistency.
// If you need generic status types, define a new type with a different name.

// export type StatusType = 'success' | 'error' | 'warning' | 'info';
